#!/usr/bin/env bash

if [ -z "$1" ]; then
  echo Please specify a PID.
  exit 1
fi

case "$(uname -s)" in
  Darwin) TAIL="gtail";;
  Linux) TAIL="tail";;
  *) echo "Unrecognized OS: $(uname -s)"; exit 1
esac

mapfile -t ARGS < <(printf -- '--pid=%s\n' "$@")

exec "$TAIL" -f "${ARGS[@]}" /dev/null
