#!/usr/bin/env sh

if [ -z "$1" ]; then
  echo Please specify a PID.
  exit 1
fi

case "$(uname -s)" in
  Darwin) TAIL="gtail";;
  Linux) TAIL="tail";;
  *) echo "Unrecognized OS: $(uname -s)"; exit 1
esac

"$TAIL" -f "--pid=$1" /dev/null
